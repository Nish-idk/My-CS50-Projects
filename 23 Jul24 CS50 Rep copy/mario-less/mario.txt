1234# row1
   ## row2
  ### row3
 #### row4
##### row5

height = number of hashes (length) of base
spaces = (height - row)                     row = line number

// make a left-aligned pyramid first, to ease things out

int main(void)
{
    height = get_int("height: ");

    for(int i = 0; i < height; i++)
    {
        printf("#")
    }
}


for(int i = 0; i < 5 - i ; i++) //make desired length of levels
    {
        printf("#");
    }

 